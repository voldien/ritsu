# Define all file lists.
FILE(GLOB FORWARD_MNIST_SOURCE_FILES  ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)

ADD_EXECUTABLE(mnist ${FORWARD_MNIST_SOURCE_FILES})
TARGET_LINK_LIBRARIES(mnist PRIVATE ritsu ritsu_datasets cxxopts)
