

# Define all file lists.
FILE(GLOB_RECURSE SOURCE_FILES  ${CMAKE_CURRENT_SOURCE_DIR}/*.h)
FILE(GLOB_RECURSE HEADER_FILES  ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)

ADD_EXECUTABLE(mnist ${SOURCE_FILES} ${HEADER_FILES} )
TARGET_LINK_LIBRARIES(mnist ritsu)
